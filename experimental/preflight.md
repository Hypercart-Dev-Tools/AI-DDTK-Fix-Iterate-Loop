## OPINIONATED PREFLIGHT START OF THE DAY/SESSION

AI-DDTK PREFLIGHT (run once at session start): Read baseline context: cat ~/bin/ai-ddtk/AGENTS.md. Probe MCP — call wpcc_list_features and local_wp_list_sites; if MCP connection fails run ./install.sh setup-mcp && ./install.sh status then retry; if MCP works but no Local sites found note "no sites configured" and continue. Select site context: local_wp_select_site on the active site, then pw_auth_status. Probe shell-only: command -v rg php node python3 git tmux. Print one-line report: MCP status, site selected (or none), auth state, missing shell tools. SESSION RULES: ALWAYS use AI-DDTK MCP tools (wpcc_*, local_wp_*, pw_auth_*, qm_*, wp_ajax_test, tmux_*) instead of ad-hoc shell equivalents. Use wpcc_run_scan for security/performance audits — rg is for code navigation only. NEVER use raw background jobs — use tmux_* tools. NEVER write ad-hoc scripts for tasks AI-DDTK already covers. Keep all runtime artifacts in ./temp. Before running any shell command, check whether an MCP tool exists for it first.